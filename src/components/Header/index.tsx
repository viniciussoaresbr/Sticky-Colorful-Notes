import React from 'react';
import { Styled } from './styles';

const Header = () => {
  return (
    <Styled.Header>
      <Styled.NoteIcon />
      Sticky Colorful Notes
    </Styled.Header>
  );
};

export default Header;
